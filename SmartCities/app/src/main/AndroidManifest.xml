<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.henallux.smartcities">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.MyActivitiesActivity" />
        <activity android:name=".view.CommentActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.RegisterActivity" />
        <activity android:name=".view.ServicesActivity" />
        <activity android:name=".view.ConnectionActivity" />
        <activity android:name=".view.SeeCommentActivity" />
        <activity android:name=".view.ProfilActivity" />
        <activity android:name=".view.LayoutActivity" />
        <activity android:name=".view.ModifProfilActivity" />
    </application>

</manifest>